<!-- display a datatable of all markets -->
<% content_for :title, 'Users' %>

<div class="content-header d-flex mb-1 justify-content-between">
  <h1 class="m-0" style="line-height: 39px;">
    Users
  </h1>
</div>
<div class="content-body">

  <section id="products-index">

    <div class="card">
      <div class="card-content">
        <div class="card-body">

          <table id="products-table" class="table table-striped" style="width:100%"
                 data-controller="app--site-admin--user--index"
                 data-app--site-admin--user--index-url-value="<%= app_site_admin_user_datatable_path %>"
                 data-app--site-admin--user--index-item-path-value="<%= app_site_admin_user_index_path %>"
          >
            <thead>
            <tr>
              <th>Name</th>
              <th>Email</th>
              <th>Last Activity At</th>
              <th>View</th>
            </tr>
            </thead>

            <tbody class="table-striped"></tbody>

            <tfoot>
            <tr>
              <th>Name</th>
              <th>Email</th>
              <th>Last Activity At</th>
              <th>View</th>
            </tr>
            </tfoot>
          </table>

        </div>
      </div>
    </div>

  </section>

</div>