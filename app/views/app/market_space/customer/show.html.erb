<% content_for :title, "Customer #{@customer.id}" %>

<%= turbo_frame_tag @customer, src: app_market_space_customer_path, loading: :lazy do %>
  <%= render SpinnerComponent.new, cached: true %>
<% end %>